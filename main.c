#include "ft_bsq.h"

int main(int    argc, char  *argv[])
{
    char map[x][y];

    if (map[x == y])

    else if (map != ) // если не равно квадрату или нельзя вывести квадрат, то :
        write (2, "map error", 9); // вывести это в отдельную функцию
}
